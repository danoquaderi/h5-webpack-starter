# Usage
```shell
# add upstream remote
$ git remote add upstream https://github.com/cycjimmy/h5-webpack-starter.git

# pull upstream branch
$ git pull --rebase upstream subaoTemplate:subaoTemplate
```

